import { NextRequest,NextResponse } from "next/server";import { createPublicClient } from "@/lib/supabase/public";
export async function GET(request:NextRequest){const db=createPublicClient();const category=request.nextUrl.searchParams.get("category");let query=db.from("products").select("id,name,slug,description,price_minor,old_price_minor,currency,is_available,is_hit,is_new,category:categories(id,name,slug),images:product_images(id,storage_path,alt_text,sort_order)").eq("is_published",true).order("sort_order");if(category)query=query.eq("categories.slug",category);const {data,error}=await query;if(error)return NextResponse.json({error:"Unable to load products"},{status:500});return NextResponse.json({data},{headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300"}})}
